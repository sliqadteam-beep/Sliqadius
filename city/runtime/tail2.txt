=.001}}
function updateBuild(dt){conG.clear();let done=false;for(let z=0;z<N;z++)for(let x=0;x<N;x++){let c=cells[idx(x,z)];if(c.p&&!c.built){c.p+=dt*.18*state.speed;if(c.p>=1){c.p=0;c.built=true;done=true}else scaff(x,z,c)}}if(done){rebuild();toast('🏢 Gebäude fertig')}}
function path(a,b){let q=[a],prev=new Map([[a.join(','),null]]);while(q.length){let n=q.shift();if(n[0]===b[0]&&n[1]===b[1]){let r=[];for(let k=n;k;k=prev.get(k.join(',')))r.push(k);return r.reverse()}for(let[dX,dZ]of[[1,0],[-1,0],[0,1],[0,-1]]){let x=n[0]+dX,z=n[1]+dZ,k=x+','+z;if(ok(x,z)&&cells[idx(x,z)].k===K.R&&!prev.has(k)){prev.set(k,n);q.push([x,z])}}}return null}function nearRoad(x,z){let best=null,bd=9;for(let zz=z-5;zz<=z+5;zz++)for(let xx=x-5;xx<=x+5;xx++)if(ok(xx,zz)&&cells[idx(xx,zz)].k===K.R){let d=Math.abs(xx-x)+Math.abs(zz-z);if(d<bd){bd=d;best=[xx,zz]}}return best}
let cars=[],npcs=[];
function wheel(){let m=new THREE.Mesh(new THREE.CylinderGeometry(.085,.085,.055,10),new THREE.MeshStandardMaterial({color:0x17191b,roughness:.95}));m.rotation.z=Math.PI/2;return m}
function carMesh(){let g=new THREE.Group(),variant=Math.random()*3|0,col=[0x2f6fb3,0xb53f42,0xd2b83c,0x3d444c,0xe7e8ea,0x274a36,0x744c8a][Math.random()*7|0],body=new THREE.MeshPhysicalMaterial({color:col,metalness:.42,roughness:.22,clearcoat:.65,clearcoatRoughness:.18}),glass=new THREE.MeshPhysicalMaterial({color:0x6f9eb5,metalness:.08,roughness:.12,transparent:true,opacity:.78,clearcoat:.8});let len=variant===0?.78:variant===1?.9:.72,w=variant===1?.5:.46,h=variant===2?.16:.19;g.add(box(w,h,len,body,0,.14,0));g.add(box(w*.78,.14,len*.42,glass,0,.28,-.05));if(variant===1)g.add(box(w*.86,.08,len*.22,body,0,.23,.27));let bumper=new THREE.MeshStandardMaterial({color:0x22272b,metalness:.45,roughness:.5});g.add(box(w*.9,.045,.035,bumper,0,.09,len*.5));g.add(box(w*.9,.045,.035,bumper,0,.09,-len*.5));let white=new THREE.MeshStandardMaterial({color:0xfff1c9,emissive:0xffe3a0,emissiveIntensity:1.2}),red=new THREE.MeshStandardMaterial({color:0xff3b30,emissive:0xaa1100,emissiveIntensity:.9});for(let s of[-1,1]){g.add(box(.09,.055,.025,white,s*w*.31,.15,-len*.505));g.add(box(.09,.05,.025,red,s*w*.31,.14,len*.505))}for(let sx of[-1,1])for(let sz of[-1,1]){let q=wheel();q.position.set(sx*w*.5,.085,sz*len*.31);g.add(q)}g.userData.kind=['Kompaktwagen','SUV','Kleinwagen'][variant];return g}
function person(){let g=new THREE.Group(),skins=[0xf1c7a5,0xd6a078,0xb97850,0x7e4f38],shirts=[0x456f9f,0x9e4e55,0x4d7c59,0xb1843f,0x6d568f,0x65717a],pants=[0x243344,0x3e4147,0x4a3a32,0x27314f],skin=new THREE.MeshStandardMaterial({color:skins[Math.random()*skins.length|0],roughness:.8}),shirt=new THREE.MeshStandardMaterial({color:shirts[Math.random()*shirts.length|0],roughness:.85}),pantsM=new THREE.MeshStandardMaterial({color:pants[Math.random()*pants.length|0],roughness:.9});let torso=box(.16,.28,.1,shirt,0,.39,0);g.add(torso);let head=new THREE.Mesh(new THREE.SphereGeometry(.095,10,8),skin);head.position.y=.61;g.add(head);let armL=new THREE.Group(),armR=new THREE.Group(),legL=new THREE.Group(),legR=new THREE.Group();armL.position.set(-.11,.48,0);armR.position.set(.11,.48,0);legL.position.set(-.045,.25,0);legR.position.set(.045,.25,0);armL.add(box(.055,.24,.055,skin,0,-.12,0));armR.add(box(.055,.24,.055,skin,0,-.12,0));legL.add(box(.065,.27,.07,pantsM,0,-.135,0));legR.add(box(.065,.27,.07,pantsM,0,-.135,0));g.add(armL,armR,legL,legR);g.scale.setScalar(.9+Math.random()*.18);g.userData={armL,armR,legL,legR,phase:Math.random()*6.28,walkSpeed:.75+Math.random()*.35};return g}
function addNpcTrip(h,d){let m=person(),A=W(...h),B=W(...d),mx=(A[0]+B[0])/2,mz=(A[1]+B[1])/2,off=(Math.random()-.5)*S*.35;m.position.set(A[0]+off,.02,A[1]+off);mobG.add(m);npcs.push({m,pts:[[A[0]+off,A[1]+off],[mx+off*.5,mz-off*.35],[B[0]+off,B[1]+off]],seg:0,t:0,speed:.42+Math.random()*.22,purpose:Math.random()<.48?'Arbeit':Math.random()<.6?'Einkaufen':'Freizeit'})}
function spawnTrip(){let homes=[],dest=[];for(let z=0;z<N;z++)for(let x=0;x<N;x++){let c=cells[idx(x,z)];if(c.built&&c.k===K.RES)homes.push([x,z]);if(c.built&&(c.k===K.IND||c.k===K.COM||c.k===K.PARK||c.k===K.HOSP))dest.push([x,z])}if(!homes.length||!dest.length||cars.length>60)return;let h=homes[Math.random()*homes.length|0],d=dest[Math.random()*dest.length|0],a=nearRoad(...h),b=nearRoad(...d);if(!a||!b)return;let manhattan=Math.abs(h[0]-d[0])+Math.abs(h[1]-d[1]);if(manhattan<7||Math.random()<.24){addNpcTrip(h,d);return}let p=path(a,b);if(!p||p.length<2)return;let metro=cells.filter(c=>c.k===K.METRO).length>=2&&Math.random()<.2;if(metro){state.metro++;addNpcTrip(h,d);return}let m=carMesh();mobG.add(m);cars.push({m,p,i:0,t:0,speed:1.25+Math.random()*.55})}
function animateWalker(a,dt){let m=a.m,u=m.userData,seg=a.seg,A=a.pts[seg],B=a.pts[seg+1];if(!B)return false;a.t+=dt*a.speed;if(a.t>=1){a.t=0;a.seg++;if(a.seg>=a.pts.length-1)return false;A=a.pts[a.seg];B=a.pts[a.seg+1]}let t=a.t,x=A[0]+(B[0]-A[0])*t,z=A[1]+(B[1]-A[1])*t;m.position.set(x,.02+Math.abs(Math.sin((u.phase+=dt*8*u.walkSpeed)))*.012,z);m.rotation.y=Math.atan2(B[0]-A[0],B[1]-A[1]);let sw=Math.sin(u.phase)*.65;u.armL.rotation.x=sw;u.armR.rotation.x=-sw;u.legL.rotation.x=-sw*.78;u.legR.rotation.x=sw*.78;return true}
function updateMob(dt){if(Math.random()<dt*.95)spawnTrip();for(let i=cars.length-1;i>=0;i--){let a=cars[i];a.t+=dt*a.speed;if(a.t>=1){a.t=0;a.i++;if(a.i>=a.p.length-1){mobG.remove(a.m);cars.splice(i,1);continue}}let A=W(...a.p[a.i]),B=W(...a.p[a.i+1]),t=a.t,e=t*t*(3-2*t),lane=.18,dx=B[0]-A[0],dz=B[1]-A[1],len=Math.hypot(dx,dz)||1,ox=-dz/len*lane,oz=dx/len*lane;a.m.position.set(A[0]+dx*e+ox,.08,A[1]+dz*e+oz);a.m.rotation.y=Math.atan2(dx,dz)}for(let i=npcs.length-1;i>=0;i--){if(!animateWalker(npcs[i],dt)){mobG.remove(npcs[i].m);npcs.splice(i,1)}}}

function serviceCount(k){return cells.filter(c=>c.k===k).length}