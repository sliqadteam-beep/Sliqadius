function covered(x,z,k,r=10){for(let zz=Math.max(0,z-r);zz<=Math.min(N-1,z+r);zz++)for(let xx=Math.max(0,x-r);xx<=Math.min(N-1,x+r);xx++)if(cells[idx(xx,zz)].k===k&&Math.abs(xx-x)+Math.abs(zz-z)<=r)return true;return false}
function updateProblems(){let changed=false;for(let z=0;z<N;z++)for(let x=0;x<N;x++){let c=cells[idx(x,z)];if(!zone(c.k)||!c.built)continue;c.waste=(c.waste||0)+.7+Math.random()*.35;if(covered(x,z,K.LANDFILL,12))c.waste=Math.max(0,c.waste-3.2);let old=c.problem||'';if(c.waste>20)c.problem='garbage';else if(old==='garbage'&&c.waste<7)c.problem='';if(!c.problem&&Math.random()<.0035&&!covered(x,z,K.HOSP,10))c.problem='health';if(!c.problem&&Math.random()<.0028&&!covered(x,z,K.POLICE,11))c.problem='crime';if(!c.problem&&Math.random()<.0015&&!covered(x,z,K.FIRE,11))c.problem='fire';if(c.problem==='health'&&covered(x,z,K.HOSP,10)&&Math.random()<.28)c.problem='';if(c.problem==='crime'&&covered(x,z,K.POLICE,11)&&Math.random()<.32)c.problem='';if(c.problem==='fire'&&covered(x,z,K.FIRE,11)&&Math.random()<.38)c.problem='';if(old!==c.problem)changed=true}if(changed)rebuild()}
function sim(){develop();updateProblems();let res=cells.filter(c=>c.k===K.RES&&c.built).length,ind=cells.filter(c=>c.k===K.IND&&c.built).length,com=cells.filter(c=>c.k===K.COM&&c.built).length,probs=cells.filter(c=>c.problem).length,svc=serviceCount(K.POLICE)+serviceCount(K.FIRE)+serviceCount(K.HOSP)+serviceCount(K.LANDFILL);state.pop+=(Math.min(res*18,(ind*20+com*14)*1.6+20)-state.pop)*.04;state.happy=Math.max(25,Math.min(96,70+cells.filter(c=>c.k===K.PARK).length*2-cars.length*.22-probs*2.7+svc*.3));state.money+=(state.pop*.18+ind*7+com*9-cells.filter(c=>c.k===K.R).length*.3-svc*2.4);$('dr').textContent=demand(K.RES)|0+'%';$('dc').textContent=demand(K.COM)|0+'%';$('di').textContent=demand(K.IND)|0+'%';$('money').textContent=Math.round(state.money).toLocaleString('de-DE');$('pop').textContent=Math.round(state.pop);$('happy').textContent=Math.round(state.happy)+'%';$('carsN').textContent=cars.length;$('npcsN').textContent=npcs.length;let lots=cells.filter(c=>c.k===K.PARKING).length*10;$('parkN').textContent=Math.min(cars.length,lots)+'/'+lots;$('metroN').textContent=state.metro;$('problemN').textContent=probs;$('serviceN').textContent=svc}
const ray=new THREE.Raycaster(),mouse=new THREE.Vector2(),plane=new THREE.Plane(new THREE.Vector3(0,1,0),0);function pick(e){let r=ren.domElement.getBoundingClientRect();mouse.x=(e.clientX-r.left)/r.width*2-1;mouse.y=-(e.clientY-r.top)/r.height*2+1;ray.setFromCamera(mouse,cam);let p=new THREE.Vector3();if(!ray.ray.intersectPlane(plane,p))r