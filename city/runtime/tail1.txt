38,.05,new THREE.MeshStandardMaterial({color:0x34383b}),wx,.28,wz+S*.37))}else if(c.k===K.LANDFILL){for(let i=0;i<4;i++){let q=box(.35,.16,.3,new THREE.MeshStandardMaterial({color:[0x817a57,0x727360,0x68685a][i%3]}),wx-.45+(i%2)*.5,.1,wz-.3+(i>>1)*.48);g.add(q)}}}
function problemSprite(txt){let c=document.createElement('canvas');c.width=256;c.height=128;let x=c.getContext('2d');x.fillStyle='rgba(15,24,30,.94)';x.beginPath();x.roundRect(4,4,248,98,20);x.fill();x.fillStyle='#fff';x.font='700 42px system-ui';x.textAlign='center';x.fillText(txt,128,67);x.fillStyle='rgba(15,24,30,.94)';x.beginPath();x.moveTo(112,100);x.lineTo(144,100);x.lineTo(128,124);x.fill();let t=new THREE.CanvasTexture(c),sp=new THREE.Sprite(new THREE.SpriteMaterial({map:t,transparent:true,depthTest:false}));sp.scale.set(2.15,1.08,1);return sp}
function rebuild(){city.clear();for(let z=0;z<N;z++)for(let x=0;x<N;x++){let c=cells[idx(x,z)];if(!c.k)continue;let g=new THREE.Group();if(c.k===K.R)road(g,x,z);else if(zone(c.k)){zonePad(g,x,z,c);if(c.built){building(g,x,z,c);if(c.problem){let[wx,wz]=W(x,z),icon=c.problem==='garbage'?'🗑️':c.problem==='health'?'🤒':c.problem==='crime'?'🚨':'🔥',sp=problemSprite(icon);sp.position.set(wx,3.15,wz);g.add(sp)}}}else service(g,x,z,c);city.add(g)}}
function scaff(x,z,c){let[wx,wz]=W(x,z),h=c.k===K.RES?1.55:c.k===K.IND?1.5:2.1,p=c.p,g=new THREE.Group(),sh=Math.max(.04,h*Math.min(1,p/.35)),w=S*.72,d=S*.72;for(let a of [-1,1])for(let b of [-1,1])g.add(box(.045,sh,.045,mat.metal,wx+a*w/2,sh/2,wz+b*d/2));for(let y=.25;y<sh;y+=.3){g.add(box(w,.035,.035,mat.metal,wx,y,wz-d/2));g.add(box(w,.035,.035,mat.metal,wx,y,wz+d/2))}if(p>.42)building(g,x,z,c);let q=Math.max(0,(p-.25)/.75),ty=h+.25-(h+.2)*q;if(p>.2&&p<.99){g.add(box(w+.12,.03,d+.12,mat.tarp,wx,ty,wz));let hh=Math.max(.12,h+.2-ty);for(let a of [-1,1]){g.add(box(.025,hh,d+.12,mat.tarp,wx+a*(w+.12)/2,ty+hh/2,wz));g.add(box(w+.12,hh,.025,mat.tarp,wx,ty+hh/2,wz+a*(d+.12)/2))}}conG.add(g)}
function demand(k){let br=cells.filter(c=>c.k===K.RES&&c.built).length,bi=cells.filter(c=>c.k===K.IND&&c.built).length,bc=cells.filter(c=>c.k===K.COM&&c.built).length;if(k===K.RES)return Math.max(8,Math.min(95,78-state.pop/Math.max(1,br*18)*30));if(k===K.COM)return Math.max(8,Math.min(95,45+state.pop/180-bc*4));return Math.max(8,Math.min(95,52+state.pop/250-bi*4))}
function develop(){let a=[];for(let z=0;z<N;z++)for(let x=0;x<N;x++){let c=cells[idx(x,z)];if([K.RES,K.IND,K.COM].includes(c.k)&&!c.built&&!c.p&&adjRoad(x,z))a.push([x,z,c])}for(let i=0;i<Math.min(3,a.length);i++){let [x,z,c]=a[(Math.random()*a.length)|0];if(Math.random()<demand(c.k)/140)c.p