
/* --- Sliqado City immersive UI/audio/detail pass --- */
const ux=document.createElement('style');ux.textContent=`
body{font-family:Inter,system-ui,Segoe UI,sans-serif}.top{top:auto!important;bottom:10px!important;left:10px!important;right:10px!important;height:50px;align-items:center;background:linear-gradient(180deg,#24343de8,#101a20f2);border:1px solid #ffffff25;border-radius:10px;padding:5px 8px;box-shadow:0 8px 28px #0009;backdrop-filter:blur(12px)}.top .pill{background:transparent!important;border:0!important;box-shadow:none!important;padding:5px 8px}.top .brand{font-size:10px;letter-spacing:.12em;color:#e7f3f7!important}.top .btn{background:#18272f;border:1px solid #ffffff18;padding:7px 9px}.cats{bottom:64px!important;background:linear-gradient(180deg,#253740ed,#132029f6)!important;border-radius:9px!important;padding:5px 7px!important;border:1px solid #ffffff25!important;box-shadow:0 9px 26px #0008!important}.cats .btn{min-width:70px;padding:7px 9px;border-radius:7px;background:transparent!important;color:#dce9ee;font-size:11px}.cats .btn.on{background:#68a95d33!important;color:#a9f38f!important;box-shadow:inset 0 -2px #7ce267}.tools{bottom:108px!important;background:#18262ee9!important;border:1px solid #ffffff22!important;border-radius:8px!important;box-shadow:0 7px 22px #0008!important;padding:5px!important}.tools .tool{background:#273a45;border:1px solid #ffffff12;min-width:88px}.tools .tool.on{background:#5f8f56!important;color:white!important;border-color:#9cdc81}.side{top:10px!important;right:10px!important;width:205px!important;background:#202b30e6!important;border:1px solid #ffffff28!important;border-radius:8px!important;box-shadow:0 10px 26px #0008!important}.side>b{display:block;font-size:13px;padding-bottom:7px;margin-bottom:5px;border-bottom:1px solid #ffffff1d}.r{font-size:11px!important;margin:4px 0!important}.hint{top:10px!important;left:10px!important;background:#1d292fcd!important;border-radius:7px!important;font-size:10px!important}.toast{top:18px!important}.city-strip{position:fixed;z-index:12;left:225px;right:230px;bottom:12px;height:44px;pointer-events:none;display:flex;align-items:center;justify-content:center;gap:18px;color:#c9d6db;font-size:10px}.city-strip b{color:white;font-size:11px}.audioctl{pointer-events:auto!important;position:fixed;z-index:14;right:226px;bottom:18px;border:1px solid #ffffff22;background:#18272f;color:#dfecef;border-radius:7px;padding:7px 9px;cursor:pointer}.audioctl.on{background:#385f3f;color:#d8ffd8}.mini-weather{position:fixed;z-index:12;left:16px;bottom:18px;font-size:12px;color:#e7f1f4;pointer-events:none}.mini-weather span{opacity:.65;font-size:10px;margin-left:4px}@media(max-width:800px){.city-strip{display:none}.audioctl{right:12px;bottom:122px}.side{top:8px!important}.top{height:46px}.cats{bottom:60px!important}.tools{bottom:102px!important}}
`;document.head.appendChild(ux);
const strip=document.createElement('div');strip.className='city-strip';strip.innerHTML='<span>📅 <b id="hudDate">Jahr 1</b></span><span>🌡 <b id="hudTemp">17°C</b></span><span>💚 <b id="hudMood">70%</b></span><span>🚗 Verkehr <b id="hudTraffic">Ruhig</b></span>';document.body.appendChild(strip);
const weather=document.createElement('div');weather.className='mini-weather';weather.innerHTML='☁️ <b>Sliqado</b><span>ruhiger Tag</span>';document.body.appendChild(weather);
const audioBtn=document.createElement('button');audioBtn.className='audioctl';audioBtn.textContent='🔇 Sound';document.body.appendChild(audioBtn);

/* richer renderer */
ren.setPixelRatio(Math.min(devicePixelRatio,2));ren.shadowMap.type=THREE.PCFSoftShadowMap;ren.toneMappingExposure=1.04;
scene.fog.density=.0048;scene.background=new THREE.Color(0xa7c4d3);

/* small city details generated around roads */
const detailG=new THREE.Group();scene.add(detailG);
const dMat={metal:new THREE.MeshStandardMaterial({color:0x3b4549,roughness:.55,metalness:.45}),lamp:new THREE.MeshStandardMaterial({color:0xffe4a8,emissive:0xffd477,emissiveIntensity:.7}),bin:new THREE.MeshStandardMaterial({color:0x42604a,roughness:.8}),sign:new THREE.MeshStandardMaterial({color:0xd9e5e7,roughness:.45,metalness:.25}),bench:new THREE.MeshStandardMaterial({color:0x6a4934,roughness:.8})};
function smallBox(w,h,d,m){return new THREE.Mesh(new THREE.BoxGeometry(w,h,d),m)}
function addLamp(wx,wz,side=1){let g=new THREE.Group(),p=new THREE.Mesh(new THREE.CylinderGeometry(.035,.045,1.7,7),dMat.metal);p.position.y=.85;let arm=smallBox(.36,.035,.035,dMat.metal);arm.position.set(.16*side,1.65,0);let l=smallBox(.16,.06,.12,dMat.lamp);l.position.set(.32*side,1.6,0);g.add(p,arm,l);g.position.set(wx,0,wz);detailG.add(g)}
function addBin(wx,wz){let b=smallBox(.24,.42,.24,dMat.bin);b.position.set(wx,.21,wz);detailG.add(b)}
function addBench(wx,wz,rot=0){let g=new THREE.Group(),seat=smallBox(.75,.08,.25,dMat.bench),back=smallBox(.75,.32,.06,dMat.bench);seat.position.y=.28;back.position.set(0,.47,.1);g.add(seat,back);g.position.set(wx,0,wz);g.rotation.y=rot;detailG.add(g)}
function addSign(wx,wz,rot=0){let g=new THREE.Group(),p=new THREE.Mesh(new THREE.CylinderGeometry(.025,.025,1.15,6),dMat.metal),s=smallBox(.32,.25,.035,dMat.sign);p.position.y=.575;s.position.y=1.05;g.add(p,s);g.position.set(wx,0,wz);g.rotation.y=rot;detailG.add(g)}
function rebuildDetails(){detailG.clear();let lamps=0;for(let z=1;z<N-1;z++)for(let x=1;x<N-1;x++){let c=cells[idx(x,z)];if(c.k!==K.R)continue;let [wx,wz]=W(x,z),h=cells[idx(x+1,z)].k===K.R||cells[idx(x-1,z)].k===K.R;if((x+z)%3===0&&lamps<520){if(h){addLamp(wx,wz+S*.43,1);addLamp(wx,wz-S*.43,-1)}else{addLamp(wx+S*.43,wz,1);addLamp(wx-S*.43,wz,-1)}lamps+=2}if((x*7+z*11)%29===0)addSign(wx+(h?0:S*.38),wz+(h?S*.38:0),h?0:Math.PI/2);if((x*13+z*5)%41===0)addBin(wx+(h?S*.36:S*.42),wz+(h?S*.42:S*.36));if((x*17+z*3)%67===0)addBench(wx+(h?0:S*.45),wz+(h?S*.48:0),h?0:Math.PI/2)}}}
const _rebuild=rebuild;rebuild=function(){_rebuild();rebuildDetails()};rebuildDetails();

/* WebAudio ambience + soft generative music. Starts only after user clicks Sound. */
let ac=null,audioOn=false,master=null,ambGain=null,musicGain=null,musicTimer=null,cityNoise=null;
function noiseBuffer(ctx,seconds=3){let b=ctx.createBuffer(1,ctx.sampleRate*seconds,ctx.sampleRate),d=b.getChannelData(0);for(let i=0;i<d.length;i++)d[i]=(Math.random()*2-1)*(.25+.75*Math.sin(i*.00007)**2);return b}
function tone(freq,dur=.5,vol=.015,type='sine',when=0){if(!ac||!audioOn)return;let o=ac.createOscillator(),g=ac.createGain();o.type=type;o.frequency.value=freq;g.gain.setValueAtTime(0,ac.currentTime+when);g.gain.linearRampToValueAtTime(vol,ac.currentTime+when+.08);g.gain.exponentialRampToValueAtTime(.0001,ac.currentTime+when+dur);o.connect(g).connect(musicGain);o.start(ac.currentTime+when);o.stop(ac.currentTime+when+dur+.05)}
function startAudio(){if(ac)return;ac=new (window.AudioContext||window.webkitAudioContext)();master=ac.createGain();master.gain.value=.72;master.connect(ac.destination);ambGain=ac.createGain();ambGain.gain.value=.06;ambGain.connect(master);musicGain=ac.createGain();musicGain.gain.value=.18;musicGain.connect(master);let src=ac.createBufferSource(),f=ac.createBiquadFilter();src.buffer=noiseBuffer(ac,5);src.loop=true;f.type='lowpass';f.frequency.value=420;src.connect(f).connect(ambGain);src.start();cityNoise=src;let hum=ac.createOscillator(),hg=ac.createGain();hum.type='sine';hum.frequency.value=54;hg.gain.value=.012;hum.connect(hg).connect(ambGain);hum.start();scheduleMusic();}
function scheduleMusic(){if(musicTimer)clearInterval(musicTimer);const chords=[[220,277.18,329.63],[196,246.94,293.66],[174.61,220,261.63],[164.81,207.65,246.94]];let n=0;musicTimer=setInterval(()=>{if(!audioOn)return;let ch=chords[n++%chords.length];ch.forEach((f,i)=>tone(f,5.8,.012,'sine',i*.08));tone(ch[0]/2,6,.008,'triangle',0)},4200)}
function clickSfx(kind='build'){if(!ac||!audioOn)return;let o=ac.createOscillator(),g=ac.createGain();o.type=kind==='doze'?'sawtooth':'triangle';o.frequency.setValueAtTime(kind==='doze'?90:240,ac.currentTime);o.frequency.exponentialRampToValueAtTime(kind==='doze'?45:120,ac.currentTime+.18);g.gain.setValueAtTime(.055,ac.currentTime);g.gain.exponentialRampToValueAtTime(.0001,ac.currentTime+.22);o.connect(g).connect(master);o.start();o.stop(ac.currentTime+.23)}
audioBtn.onclick=async()=>{if(!ac)startAudio();if(ac.state==='suspended')await ac.resume();audioOn=!audioOn;master.gain.setTargetAtTime(audioOn?.72:.0001,ac.currentTime,.05);audioBtn.textContent=audioOn?'🔊 Sound':'🔇 Sound';audioBtn.classList.toggle('on',audioOn)};
ren.domElement.addEventListener('pointerup',e=>{if(e.button===0&&audioOn)clickSfx(cat==='bulldoze'?'doze':'build')});
setInterval(()=>{if(!audioOn||!ac)return;let moving=(typeof cars!=='undefined'?cars.length:0);ambGain.gain.setTargetAtTime(.035+Math.min(.055,moving*.0015),ac.currentTime,1.2);if(Math.random()<.22&&moving>2){let o=ac.createOscillator(),g=ac.createGain();o.type='triangle';o.frequency.value=95+Math.random()*55;g.gain.setValueAtTime(.006,ac.currentTime);g.gain.exponentialRampToValueAtTime(.0001,ac.currentTime+.8);o.connect(g).connect(ambGain);o.start();o.stop(ac.currentTime+.9)}},1700);

/* small HUD updates */
let uiYear=1;setInterval(()=>{uiYear++;let hd=$('hudDate'),hm=$('hudMood'),ht=$('hudTraffic'),temp=$('hudTemp');if(hd)hd.textContent='Jahr '+Math.max(1,Math.floor(uiYear/10));if(hm)hm.textContent=Math.round(state.happy)+'%';if(ht){let n=typeof cars!=='undefined'?cars.length:0;ht.textContent=n>35?'Dicht':n>15?'Mittel':'Ruhig'}if(temp)temp.textContent=(14+Math.round(Math.sin(uiYear*.13)*5))+'°C'},2500);
