
/* Sliqado City - automatic demolition of obstructing buildings */
const __emptyCell=()=>({k:0,built:false,p:0,l:1,v:0,waste:0,problem:''});
function __clearGridNearWorld(x,z,r,{keepRoads=true}={}){let removed=0,minX=Math.max(0,Math.floor((x-r+H)/S)-1),maxX=Math.min(N-1,Math.ceil((x+r+H)/S)+1),minZ=Math.max(0,Math.floor((z-r+H)/S)-1),maxZ=Math.min(N-1,Math.ceil((z+r+H)/S)+1);for(let gz=minZ;gz<=maxZ;gz++)for(let gx=minX;gx<=maxX;gx++){let [wx,wz]=W(gx,gz);if(Math.hypot(wx-x,wz-z)>r+S*.55)continue;let ii=idx(gx,gz),c=cells[ii];if(!c||!c.k)continue;if(keepRoads&&c.k===K.R)continue;cells[ii]=__emptyCell();freeRaster.delete(ii);removed++}return removed}
function __clearGridAlongSegment(a,b,width){let removed=0;for(let z=0;z<N;z++)for(let x=0;x<N;x++){let ii=idx(x,z),c=cells[ii];if(!c||!c.k||c.k===K.R)continue;let [wx,wz]=W(x,z),q=segClosest({x:wx,z:wz},a,b);if(q.d<=width*.5+S*.46){cells[ii]=__emptyCell();freeRaster.delete(ii);removed++}}return removed}
function __clearFreeServicesAlongSegment(a,b,width){let before=freeServices.length;freeServices=freeServices.filter(s=>segClosest({x:s.x,z:s.z},a,b).d>width*.5+1.25);return before-freeServices.length}
const __rasterRoad=rasterRoad;rasterRoad=function(r){let spec=roadSpecs[r.type]||roadSpecs.road,removed=0;for(let i=0;i<r.p.length-1;i++){removed+=__clearGridAlongSegment(r.p[i],r.p[i+1],spec.width);removed+=__clearFreeServicesAlongSegment(r.p[i],r.p[i+1],spec.width)}__rasterRoad(r);if(removed)queueMicrotask(()=>toast('🧨 Hindernisse automatisch abgerissen'))};
const __placeManual=placeManual;placeManual=function(p,k){let nr=nearestRoad(p,4.2);if(!nr){toast('⚠️ Dieses Gebäude muss an einer Straße stehen');return}let d=Object.values(defs).flat().find(v=>v[2]===k),cost=d?d[3]:0;if(state.money<cost){toast('💸 Nicht genug Geld');return}let dx=nr.b.x-nr.a.x,dz=nr.b.z-nr.a.z,L=Math.hypot(dx,dz)||1,nx=-dz/L,nz=dx/L,side=((p.x-nr.x)*nx+(p.z-nr.z)*nz)>=0?1:-1,off=nr.width*.55+1.25,x=nr.x+nx*off*side,z=nr.z+nz*off*side;let removed=__clearGridNearWorld(x,z,1.35,{keepRoads:true});let before=freeServices.length;freeServices=freeServices.filter(s=>Math.hypot(s.x-x,s.z-z)>1.7);removed+=before-freeServices.length;__placeManual(p,k);if(removed){rebuild();rebuildFreeRoads();saveFree();toast('🧨 Gebäude im Weg automatisch abgerissen')}};
const __apply=apply;apply=function(ps){if(cat==='nature'){let d=defs[cat]&&defs[cat].find(x=>x[0]===tool);if(d&&d[2]===K.PARK){for(let p of ps){let c=cells[idx(p.x,p.z)];if(c&&c.k&&c.k!==K.R)cells[idx(p.x,p.z)]=__emptyCell()}}}return __apply(ps)};
