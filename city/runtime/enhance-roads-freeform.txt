
/* Sliqado City - freeform road/building placement */
const freeRoadG=new THREE.Group(),freePreviewG=new THREE.Group(),freeServiceG=new THREE.Group();scene.add(freeRoadG,freePreviewG,freeServiceG);
const FREEKEY='sliqadoCityFreeRoadsV1';
let freeRoads=[],freeServices=[],freeRaster=new Set(),freeDrag=null;
const roadSpecs={road:{name:'2-Spur',width:1.35,cost:95,lanes:2},oneway:{name:'Einbahnstraße',width:1.15,cost:105,lanes:1,oneway:true},road4:{name:'4-Spur',width:2.35,cost:190,lanes:4},highway:{name:'Autobahn',width:3.0,cost:330,lanes:4,highway:true},roundSmall:{name:'Kleiner Kreisverkehr',width:1.2,cost:280,round:3.2},roundLarge:{name:'Großer Kreisverkehr',width:1.55,cost:620,round:5.6}};
defs.roads=[['road','2-Spur Straße',K.R,95],['oneway','↗ Einbahnstraße',K.R,105],['road4','4-Spur Straße',K.R,190],['highway','🛣 Autobahn',K.R,330],['roundSmall','↻ Kleiner Kreisverkehr',K.R,280],['roundLarge','↻ Großer Kreisverkehr',K.R,620]];
const manualKs=new Set([K.METRO,K.PARKING,K.POLICE,K.FIRE,K.HOSP,K.LANDFILL]);
function worldPick(e){let r=ren.domElement.getBoundingClientRect(),m=new THREE.Vector2((e.clientX-r.left)/r.width*2-1,-((e.clientY-r.top)/r.height*2-1)),rr=new THREE.Raycaster(),p=new THREE.Vector3();rr.setFromCamera(m,cam);return rr.ray.intersectPlane(new THREE.Plane(new THREE.Vector3(0,1,0),0),p)?{x:p.x,z:p.z}:null}
function segClosest(p,a,b){let vx=b.x-a.x,vz=b.z-a.z,l=vx*vx+vz*vz;if(!l)return{x:a.x,z:a.z,d:Math.hypot(p.x-a.x,p.z-a.z),t:0};let t=Math.max(0,Math.min(1,((p.x-a.x)*vx+(p.z-a.z)*vz)/l)),x=a.x+vx*t,z=a.z+vz*t;return{x,z,d:Math.hypot(p.x-x,p.z-z),t}}
function snapPoint(p,max=1.25){let best={x:p.x,z:p.z,d:max};for(let r of freeRoads){for(let q of [r.p[0],r.p[r.p.length-1]]){let d=Math.hypot(p.x-q.x,p.z-q.z);if(d<best.d)best={x:q.x,z:q.z,d}}for(let i=0;i<r.p.length-1;i++){let q=segClosest(p,r.p[i],r.p[i+1]);if(q.d<best.d)best=q}}return{x:best.x,z:best.z,snapped:best.d<max}}
function lineMesh(a,b,w,material,y=.07){let dx=b.x-a.x,dz=b.z-a.z,len=Math.hypot(dx,dz);if(len<.05)return null;let o=box(w,.055,len,material,(a.x+b.x)/2,y,(a.z+b.z)/2);o.rotation.y=Math.atan2(dx,dz);return o}
const freeAsphalt=new THREE.MeshStandardMaterial({map:mat.road.map,color:0xffffff,roughness:.94,metalness:.02});
const stripeMat=new THREE.MeshStandardMaterial({color:0xf4f0d8,roughness:.75}),whiteMat=new THREE.MeshStandardMaterial({color:0xf4f4ef,roughness:.72});
function drawRoadSegment(g,a,b,spec){let main=lineMesh(a,b,spec.width,freeAsphalt,.085);if(main)g.add(main);let dx=b.x-a.x,dz=b.z-a.z,len=Math.hypot(dx,dz),nx=-dz/len,nz=dx/len;for(let side of[-1,1]){let aa={x:a.x+nx*spec.width*.52*side,z:a.z+nz*spec.width*.52*side},bb={x:b.x+nx*spec.width*.52*side,z:b.z+nz*spec.width*.52*side},curb=lineMesh(aa,bb,.09,mat.side,.1);if(curb)g.add(curb)}
 if(spec.lanes===2&&!spec.oneway){let c=lineMesh(a,b,.035,stripeMat,.12);if(c)g.add(c)}
 if(spec.lanes===4){for(let off of[-.34,.34]){let aa={x:a.x+nx*off,z:a.z+nz*off},bb={x:b.x+nx*off,z:b.z+nz*off},c=lineMesh(aa,bb,.028,whiteMat,.12);if(c)g.add(c)}let c=lineMesh(a,b,.075,spec.highway?whiteMat:stripeMat,.125);if(c)g.add(c)}
 if(spec.oneway){for(let t=.25;t<.9;t+=.45){let x=a.x+(b.x-a.x)*t,z=a.z+(b.z-a.z)*t;let ar=box(.18,.025,.34,whiteMat,x,.125,z);ar.rotation.y=Math.atan2(dx,dz);g.add(ar)}}}
function roadVisual(r,g=freeRoadG){let spec=roadSpecs[r.type]||roadSpecs.road;for(let i=0;i<r.p.length-1;i++)drawRoadSegment(g,r.p[i],r.p[i+1],spec)}
function rebuildFreeRoads(){freeRoadG.clear();for(let r of freeRoads)roadVisual(r);freeServiceG.clear();for(let s of freeServices)drawFreeService(s)}
function rasterRoad(r){let spec=roadSpecs[r.type]||roadSpecs.road;for(let i=0;i<r.p.length-1;i++){let a=r.p[i],b=r.p[i+1],len=Math.hypot(b.x-a.x,b.z-a.z),steps=Math.max(2,Math.ceil(len/.55));for(let j=0;j<=steps;j++){let t=j/steps,x=a.x+(b.x-a.x)*t,z=a.z+(b.z-a.z)*t,gx=Math.floor((x+H)/S),gz=Math.floor((z+H)/S);if(ok(gx,gz)){let ii=idx(gx,gz),c=cells[ii];if(!c.k||c.k===K.R){c.k=K.R;c.built=true;c.fr=1;freeRaster.add(ii)}}}}}
const baseRoadDraw=road;road=function(g,x,z){let c=cells[idx(x,z)];if(c&&c.fr)return;baseRoadDraw(g,x,z)};
function addFreeRoad(points,type){let spec=roadSpecs[type],len=0;for(let i=0;i<points.length-1;i++)len+=Math.hypot(points[i+1].x-points[i].x,points[i+1].z-points[i].z);let cost=Math.ceil(len*spec.cost/2.2);if(state.money<cost){toast('💸 Nicht genug Geld');return false}state.money-=cost;let r={type,p:points.map(q=>({x:q.x,z:q.z}))};freeRoads.push(r);rasterRoad(r);rebuild();rebuildFreeRoads();saveFree();toast('🛣 Straße gebaut');return true}
function addRound(center,type){let spec=roadSpecs[type],R=spec.round,n=28,p=[];for(let i=0;i<=n;i++){let a=i/n*Math.PI*2;p.push({x:center.x+Math.cos(a)*R,z:center.z+Math.sin(a)*R})}if(!addFreeRoad(p,type))return;for(let a of[0,Math.PI/2,Math.PI,Math.PI*1.5]){let q1={x:center.x+Math.cos(a)*R,z:center.z+Math.sin(a)*R},q2={x:center.x+Math.cos(a)*(R+3.0),z:center.z+Math.sin(a)*(R+3.0)};addFreeRoad([q1,q2],'oneway')}}
function previewRoad(a,b,type){freePreviewG.clear();let g=new THREE.Group(),spec=roadSpecs[type]||roadSpecs.road;drawRoadSegment(g,a,b,spec);g.traverse(o=>{if(o.material){o.material=o.material.clone();o.material.transparent=true;o.material.opacity=.58}});freePreviewG.add(g)}
function nearestRoad(p,max=4.5){let best=null;for(let r of freeRoads){let spec=roadSpecs[r.type]||roadSpecs.road;for(let i=0;i<r.p.length-1;i++){let q=segClosest(p,r.p[i],r.p[i+1]);if(q.d<max&&(!best||q.d<best.d)){let a=r.p[i],b=r.p[i+1];best={...q,a,b,width:spec.width}}}}if(!best){for(let z=0;z<N;z++)for(let x=0;x<N;x++)if(cells[idx(x,z)].k===K.R&&!cells[idx(x,z)].fr){let [wx,wz]=W(x,z),d=Math.hypot(p.x-wx,p.z-wz);if(d<max&&(!best||d<best.d))best={x:wx,z:wz,d,a:{x:wx-1,z:wz},b:{x:wx+1,z:wz},width:1.35}}}return best}
function drawFreeService(s){let g=new THREE.Group(),fx=(s.x+H)/S-.5,fz=(s.z+H)/S-.5,c={k:s.k,built:true,v:s.v||0};service(g,fx,fz,c);freeServiceG.add(g)}
function placeManual(p,k){let nr=nearestRoad(p,4.2);if(!nr){toast('⚠️ Dieses Gebäude muss an einer Straße stehen');return}let dx=nr.b.x-nr.a.x,dz=nr.b.z-nr.a.z,L=Math.hypot(dx,dz)||1,nx=-dz/L,nz=dx/L,side=((p.x-nr.x)*nx+(p.z-nr.z)*nz)>=0?1:-1,off=nr.width*.55+1.25,x=nr.x+nx*off*side,z=nr.z+nz*off*side;let d=Object.values(defs).flat().find(v=>v[2]===k),cost=d?d[3]:0;if(state.money<cost){toast('💸 Nicht genug Geld');return}state.money-=cost;freeServices.push({x,z,k,v:(Math.random()*12)|0});rebuildFreeRoads();saveFree();toast('🏗 An Straße platziert')}
const oldCovered=covered;covered=function(x,z,k,r=10){if(oldCovered(x,z,k,r))return true;return freeServices.some(s=>s.k===k&&Math.hypot(((s.x+H)/S-.5)-x,((s.z+H)/S-.5)-z)<=r)};
const oldServiceCount=serviceCount;serviceCount=function(k){return oldServiceCount(k)+freeServices.filter(s=>s.k===k).length};
const oldSim=sim;sim=function(){oldSim();let extraLots=freeServices.filter(s=>s.k===K.PARKING).length*10;if(extraLots){let base=cells.filter(c=>c.k===K.PARKING).length*10;$('parkN').textContent=Math.min(cars.length,base+extraLots)+'/'+(base+extraLots)}};
function saveFree(){localStorage.setItem(FREEKEY,JSON.stringify({roads:freeRoads,services:freeServices}))}
try{let o=JSON.parse(localStorage.getItem(FREEKEY)||'{}');freeRoads=Array.isArray(o.roads)?o.roads:[];freeServices=Array.isArray(o.services)?o.services:[];for(let r of freeRoads)rasterRoad(r)}catch{}
$('save').addEventListener('click',saveFree);
ren.domElement.addEventListener('pointerdown',e=>{if(e.button!==0)return;let p=worldPick(e);if(!p)return;if(cat==='roads'){e.preventDefault();e.stopImmediatePropagation();if(tool==='roundSmall'||tool==='roundLarge'){addRound(snapPoint(p,.9),tool);return}let s=snapPoint(p);freeDrag={a:s,type:tool};previewRoad(s,s,tool);return}let d=defs[cat]&&defs[cat].find(x=>x[0]===tool);if(d&&manualKs.has(d[2])){e.preventDefault();e.stopImmediatePropagation();placeManual(p,d[2])}},true);
ren.domElement.addEventListener('pointermove',e=>{if(!freeDrag)return;e.preventDefault();e.stopImmediatePropagation();let p=worldPick(e);if(p)previewRoad(freeDrag.a,snapPoint(p),freeDrag.type)},true);
ren.domElement.addEventListener('pointerup',e=>{if(!freeDrag)return;e.preventDefault();e.stopImmediatePropagation();let p=worldPick(e),a=freeDrag.a,b=p?snapPoint(p):a,type=freeDrag.type;freeDrag=null;freePreviewG.clear();if(Math.hypot(b.x-a.x,b.z-a.z)>.35)addFreeRoad([a,b],type)},true);
// free-road bulldozer: remove the closest custom road
ren.domElement.addEventListener('dblclick',e=>{if(cat!=='bulldoze')return;let p=worldPick(e),best=-1,bd=1.7;for(let ri=0;ri<freeRoads.length;ri++){let r=freeRoads[ri];for(let i=0;i<r.p.length-1;i++){let q=segClosest(p,r.p[i],r.p[i+1]);if(q.d<bd){bd=q.d;best=ri}}}if(best>=0){freeRoads.splice(best,1);freeRaster.clear();cells.forEach(c=>{if(c.fr){c.k=0;c.fr=0}});for(let r of freeRoads)rasterRoad(r);rebuild();rebuildFreeRoads();saveFree();toast('Straße entfernt')}},true);
let freeHint=document.createElement('div');freeHint.className='p';freeHint.style.cssText='position:fixed;left:10px;bottom:118px;z-index:11;padding:7px 10px;border-radius:9px;font-size:11px;pointer-events:none';freeHint.textContent='Straßen: frei ziehen · Enden rasten automatisch ein · Kreisverkehr: einmal klicken';document.body.appendChild(freeHint);rebuildFreeRoads();
