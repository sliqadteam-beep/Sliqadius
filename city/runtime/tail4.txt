eturn null;let x=Math.floor((p.x+H)/S),z=Math.floor((p.z+H)/S);return ok(x,z)?{x,z}:null}function line(a,b){let o=[],x=a.x,z=a.z,dx=Math.abs(b.x-x),dz=Math.abs(b.z-z),sx=x<b.x?1:-1,sz=z<b.z?1:-1,e=dx-dz;for(;;){o.push({x,z});if(x===b.x&&z===b.z)break;let e2=2*e;if(e2>-dz){e-=dz;x+=sx}if(e2<dx){e+=dx;z+=sz}}return o}
function apply(ps){let d=defs[cat].find(x=>x[0]===tool);for(let p of ps){let c=cells[idx(p.x,p.z)];if(cat==='bulldoze'){cells[idx(p.x,p.z)]={k:0,built:false,p:0,l:1,v:0,waste:0,problem:''};continue}if(c.k)continue;if(zone(d[2])&&!adjRoad(p.x,p.z))continue;if(state.money<d[3])continue;state.money-=d[3];cells[idx(p.x,p.z)]={k:d[2],built:!zone(d[2]),p:0,l:1,v:(Math.random()*12)|0,waste:0,problem:''}}rebuild()}
let drag=null,orbit=false,last={x:0,y:0},az=.78,el=.78,dist=78,target=new THREE.Vector3();ren.domElement.oncontextmenu=e=>e.preventDefault();ren.domElement.addEventListener('pointerdown',e=>{last={x:e.clientX,y:e.clientY};if(e.button===2){orbit=true;return}drag=pick(e)});ren.domElement.addEventListener('pointermove',e=>{if(orbit){az-=(e.clientX-last.x)*.006;el=Math.max(.25,Math.min(1.35,el+(e.clientY-last.y)*.006));last={x:e.clientX,y:e.clientY}}});ren.domElement.addEventListener('pointerup',e=>{if(orbit){orbit=false;return}if(drag){let b=pick(e)||drag;apply(line(drag,b));drag=null}});ren.domElement.addEventListener('wheel',e=>dist=Math.max(24,Math.min(150,dist+e.deltaY*.05)),{passive:true});
function renderUI(){cats.innerHTML='';for(let c of catsDef){let b=document.createElement('button');b.className='btn '+(c[0]===cat?'on':'');b.textContent=c[1];b.onclick=()=>{cat=c[0];tool=defs[cat][0][0];renderUI()};cats.appendChild(b)}tools.innerHTML='';for(let t of defs[cat]){let b=document.createElement('button');b.className='tool '+(t[0]===tool?'on':'');b.textContent=t[1];b.onclick=()=>{tool=t[0];renderUI()};tools.appendChild(b)}}function toast(t){let e=$('toast');e.textContent=t;e.style.opacity=1;clearTimeout(e.t);e.t=setTimeout(()=>e.style.opacity=0,1300)}$('pause').onclick=()=>{state.paused=!state.paused;$('pause').textContent=state.paused?'▶':'⏸'};$('speed').onclick=()=>{state.speed=state.speed===1?2:state.speed===2?4:1;$('speed').textContent=state.speed+'×'};$('save').onclick=()=>{localStorage.setItem('sliqadoCityCompact',JSON.stringify({cells,state}));toast('Gespeichert')};let v=localStorage.getItem('sliqadoCityCompact');if(v)try{let o=JSON.parse(v);cells=o.cells;state={...state,...o.state};cells.forEach(c=>{c.v??=(Math.random()*12)|0;c.waste??=0;c.problem??=''})}catch{}if(!cells.some(c=>c.k)){let z=N>>1;for(let x=z-8;x<=z+8;x++)cells[idx(x,z