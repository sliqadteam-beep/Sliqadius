ion toast(t){let e=$('toast');e.textContent=t;e.style.opacity=1;clearTimeout(e.t);e.t=setTimeout(()=>e.style.opacity=0,1300)}function demolish(p,w){buildings=buildings.filter(b=>{for(let i=0;i<p.length-1;i++)if(seg(b,p[i],p[i+1]).d<w*.55+1.4)return false;return true});buildingsBuild()}function roadLen(p){let l=0;for(let i=0;i<p.length-1;i++)l+=Math.hypot(p[i+1].x-p[i].x,p[i+1].z-p[i].z);return l}function addRoad(p,t){let s=RT[t];if(!s||!Array.isArray(p)||p.length<2)return false;let l=roadLen(p);if(l<.45)return toast('Road is too short');let c=Math.max(1,Math.ceil(l*s.c/2.2));if(st.money<c)return toast('Not enough money');demolish(p,s.w);st.money-=c;roads.push({t,p:p.map(q=>({x:+q.x,z:+q.z}))});roadsBuild();saveGame();toast('Road built');return true}function round(p,t){let s=RT[t];if(!s||!s.r)return;let parts=[],a=[];for(let i=0;i<=36;i++){let q=i/36*Math.PI*2;a.push({x:p.x+Math.cos(q)*s.r,z:p.z+Math.sin(q)*s.r})}parts.push({t,p:a});for(let q of[0,Math.PI/2,Math.PI,Math.PI*1.5])parts.push({t:'one',p:[{x:p.x+Math.cos(q)*s.r,z:p.z+Math.sin(q)*s.r},{x:p.x+Math.cos(q)*(s.r+4),z:p.z+Math.sin(q)*(s.r+4)}]});let cost=parts.reduce((n,r)=>n+Math.max(1,Math.ceil(roadLen(r.p)*RT[r.t].c/2.2)),0);if(st.money<cost)return toast('Not enough money');for(let r of parts)demolish(r.p,RT[r.t].w);st.money-=cost;roads.push(...parts);roadsBuild();saveGame();toast('Roundabout built')}function place(p,k,c=0){if(!p||!Number.isFinite(p.x)||!Number.isFinite(p.z))return;let n=near(p);if(!n)return toast('Must be placed next to a road');if(st.money<c)return toast('Not enough money');let dx=n.b.x-n.a.x,dz=n.b.z-n.a.z,l=Math.hypot(dx,dz)||1,nx=-dz/l,nz=dx/l,s=((p.x-n.x)*nx+(p.z-n.z)*nz)>=0?1:-1,x=n.x+nx*(n.w*.55+2.1)*s,z=n.z+nz*(n.w*.55+2.1)*s;buildings=buildings.filter(b=>Math.hypot(b.x-x,b.z-z)>1.8);st.money-=c;buildings.push({x,z,k,level:1});buildingsBuild();saveGame();toast('Placed')}const D={roads:[['road','2-Lane Road',95],['one','One-Way Road',105],['road4','4-Lane Road',190],['highway','Highway',330],['rs','Small Roundabout',280],['rl','Large Roundabout',620]],zones:[['res','Residential',0],['com','Commercial',0],['ind','Industrial',0]],services:[['police','Police Station',18000],['fire','Fire Station',20000],['hospital','Hospital',26000],['landfill','Landfill',14000]],transport:[['metro','Metro Station',25000],['parking','Parking Lot',1500]],nature:[['park','Park',800]],bulldoze:[['doze','Bulldoze',0]]},CATS=[['inspect','Inspect'],['roads','Roads'],['zones','Zones'],['services','Services'],['transport','Transport'],['nature','Nature'],['bulldoze','Bulldoze']];let cat='roads',tool='road';function ui(){let c=$('cats');c.innerHTML='';for(let a of CATS){let b=document.createElement('button');b.className='cat '+(cat===a[0]?'on':'');b.innerHTML='<i>'+a[1][0]+'</i>'+a[1];b.onclick=()=>{cat=a[0];if(cat!=='inspect')tool=D[cat][0][0];ui()};c.appendChild(b)}let t=$('tools');t.innerHTML='';if(cat==='inspect'){t.textContent='Click a building to inspect it';return}for(let d of D[cat]){let b=document.createElement('button');b.className='tool '+(tool===d[0]?'on':'');b.textContent=d[1]+(d[2]?' '+d[2].toLocaleString():'');b.onclick=()=>{tool=d[0];ui()};t.appendChild(b)}}ui();const ray=new T.Raycaster(),mo=new T.Vector2(),pl=new T.Plane(new T.Vector3(0,1,0),0);function pick(e){let r=R.domElement.getBoundingClientRect();mo.x=(e.clientX-r.left)/r.width*2-1;mo.y=-((e.clientY-r.top)/r.height*2-1);ray.setFromCamera(mo,cam);let p=new T.Vector3();return ray.ray.intersectPlane(pl,p)?{x:p.x,z:p.z}:null}let az=.78,el=.78,dist=78,target=new T.Vector3(),orbit=0,last={},drag=null;R.domElement.oncontextmenu=e=>e.preventDefault();function beginInput(e,p){last={x:e.clientX,y:e.clientY};if(e.button===2){orbit=1;return}if(!p)return;if(cat==='roads'){if(tool==='rs'||tool==='rl')return round(snap(p),tool);drag={a:snap(p),t:tool}}else if(cat==='zones')place(p,tool);else if(cat==='services'||cat==='transport'||cat==='nature'){let d=D[cat].find(x=>x[0]===tool);if(d)place(p,tool,d[2])}else if(cat==='bulldoze'){let n=near(p,2);if(n){let rr=n.r;roads.splice(roads.indexOf(rr),1);carsForRoadGone(rr);roadsBuild()}else{buildings=buildings.filter(b=>Math.hypot(b.x-p.x,b.z-p.z)>2);buildingsBuild()}saveGame()}else if(cat==='inspect'){let b=[...buildings].sort((a,b)=>Math.hypot(a.x-p.x,a.z-p.z)-Math.hypot(b.x-p.x,b.z-p.z))[0];if(b&&Math.hypot(b.x-p.x,b.z-p.z)<3)show(b)}}R.domElement.onpointerdown=e=>{if(e.pointerType==='touch')return;beginInput(e,pick(e))};R.domElement.onpointermove=e=>{if(orbit){az-=(e.clientX-last.x)*.006;el=C(el+(e.clientY-last.y)*.006,.25,1.35);last={x:e.clientX,y:e.clientY}}else if(drag){let p=pick(e);if(p){pg.clear();draw({t:drag.t,p:[drag.a,snap(p)]},pg,.45)}}};R.domElement.onpointerup=e=>{if(e.button===2){orbit=0;return}if(drag){let p=pick(e),d=drag;drag=null;pg.clear();if(p){let b=snap(p);if(Math.hypot(b.x-d.a.x,b.z-d.a.z)>=.45)addRoad([d.a,b],d.t)}}};R.domElement.onpointercancel=()=>{orbit=0;drag=null;pg.clear()};R.domElement.onpointerleave=e=>{if(e.buttons=