==0){orbit=0;if(drag){drag=null;pg.clear()}}};R.domElement.onwheel=e=>dist=C(dist+e.deltaY*.05,24,145);function show(b){$('it').textContent=({res:'Residential Building',com:'Commercial Building',ind:'Industrial Building',park:'Park',police:'Police Station',fire:'Fire Station',hospital:'Hospital',landfill:'Landfill',metro:'Metro Station',parking:'Parking Lot'})[b.k]||'Building';$('ib').innerHTML='<b>Operating normally</b><p>Road access</p><div class="bar"><i style="width:100%"></i></div><div class="grid"><div class="card">Level<b>'+(b.level||1)+'</b></div><div class="card">Service status<b>Good</b></div><div class="card">Land value<b>'+Math.round(45+st.happy*.4)+'</b></div><div class="card">Workers / residents<b>'+(['res','com','ind'].includes(b.k)?8:0)+'</b></div></div>';$('info').style.display='block'}$('info').onclick=()=>$('info').style.display='none';function seed(){roads=[{t:'road4',p:[{x:-30,z:0},{x:30,z:0}]},{t:'road',p:[{x:-18,z:-24},{x:-18,z:24}]},{t:'road',p:[{x:0,z:-24},{x:0,z:24}]},{t:'road',p:[{x:18,z:-24},{x:18,z:24}]}];for(let x of[-24,-12,7,24])for(let z of[-7,7])buildings.push({x,z,k:x<0?'res':x>15?'ind':'com',level:1});buildings.push({x:-7,z:7,k:'park'},{x:8,z:-7,k:'parking'})}function safeBuilding(b){return b&&typeof b.k==='string'&&Number.isFinite(b.x)&&Number.isFinite(b.z)&&['res','com','ind','park','police','fire','hospital','landfill','metro','parking'].includes(b.k)}function saveGame(){try{localStorage.setItem('sliqadiusCityV2',JSON.stringify({version:3,st:{money:st.money,pop:st.pop,happy:st.happy,speed:st.speed,paused:st.paused},roads:roads.filter(validRoad),buildings:buildings.filter(safeBuilding)}))}catch(e){toast('Save failed')}}try{let raw=localStorage.getItem('sliqadiusCityV2'),o=raw?JSON.parse(raw):null;if(o&&typeof o==='object'){if(o.st&&typeof o.st==='object')st={...st,...o.st};st.money=Number.isFinite(+st.money)?+st.money:100000;st.pop=Number.isFinite(+st.pop)?+st.pop:0;st.happy=C(Number.isFinite(+st.happy)?+st.happy:70,0,100);st.speed=[1,2,4].includes(+st.speed)?+st.speed:1;st.paused=true;roads=Array.isArray(o.roads)?o.roads.filter(validRoad):[];buildings=Array.isArray(o.buildings)?o.buildings.filter(safeBuilding):[];if(!roads.length&&!buildings.length)seed()}else seed()}catch(e){roads=[];buildings=[];seed()}roadsBuild();buildingsBuild();function car(){let usable=roads.filter(validRoad);if(!usable.length||cars.length>=40)return;let r=usable[Math.floor(Math.random()*usable.length)],g=new T.Group();g.add(B(.65,.25,1.15,new T.MeshStandardMaterial({color:[0x315f8c,0xb34f45,0xe0ddcf,0x45484a][Math.floor(Math.random()*4)]}),0,.25,0),B(.5,.22,.55,gl,0,.46,0));mg.add(g);cars.push({g,r,i:0,t:0,s:.15+Math.random()*.08})}function carsUp(dt){if(Math.random()<dt*.7*st.speed)car();for(let x=cars.length-1;x>=0;x--){let c=cars[x];if(!validRoad(c.r)||!roads.includes(c.r)){removeCar(c);cars.splice(x,1);continue}let max=c.r.p.length-1;c.i=C(c.i|0,0,max-1);let a=c.r.p[c.i],b=c.r.p[c.i+1];if(!a||!b){removeCar(c);cars.splice(x,1);continue}c.t+=dt*c.s*st.speed;while(c.t>=1){c.t-=1;c.i=(c.i+1)%max;a=c.r.p[c.i];b=c.r.p[c.i+1]}c.g.position.set(a.x+(b.x-a.x)*c.t,.03,a.z+(b.z-a.z)*c.t);c.g.rotation.y=Math.atan2(b.x-a.x,b.z-a.z)}}let ac,on=0,master,music,city;try{let ap=JSON.parse(localStorage.getItem('sliqadiusAudioV1')||'{}');if(Number.isFinite(ap.mv))$('mv').value=ap.mv;if(Number.isFinite(ap.mus))$('mus').value=ap.mus;if(Number.isFinite(ap.cv))$('cv').value=ap.cv;for(let id of['mv','mus','cv']){let v={mv:'mvv',mus:'musv',cv:'cvv'};$(v[id]).textContent=$(id).value+'%'}}catch{}function audio(){if(ac)return;let AC=window.AudioContext||window.webkitAudioContext;if(!AC)return toast('Audio is not supported');ac=new AC;master=ac.createGain();music=ac.createGain();city=ac.createGain();music.connect(master);city.connect(master);master.connect(ac.destination);let o=ac.createOscillator(),g=ac.createGain();o.frequency.value=55;g.gain.value=.01;o.connect(g).connect(city);o.start();setInterval(()=>{if(!on)return;let a=ac.createOscillator(),x=ac.createGain();a.frequency.value=[165,196,220,247][Math.floor(Math.random()*4)];x.gain.value=.015;a.connect(x).connect(music);a.start();x.gain.exponentialRampToValueAtTime(.0001,ac.currentTime+4);a.stop(ac.currentTime+4)},4200);vol()}function vol(){if(!ac)return;master.gain.value=on?+$('mv').value/100:0;music.gain.value=+$('mus').value/100;city.gain.value=+$('cv').value/100}$('snd').onclick=()=>{audio();on=!on;$('snd').textContent='Sound: '+(on?'On':'Off');vol()};for(let id of['mv','mus','cv'])$(id).oninput=()=>{let v={mv:'mvv',mus:'musv',cv:'cvv'};$(v[id]).textContent=$(id).value+'%';try{localStorage.setItem('sliqadiusAudioV1',JSON.stringify({mv:+$('mv').value,mus:+$('mus').value,cv:+$('cv').value}))}catch{}vol()};$('play').onclick=()=>{$('menu').style.display='none';st.paused=false};$('set').onclick=()=>{$('menu').style.display='none';$('settings').style.display='flex'};$('back').onclick=()=>{$('settings').style.display='none';$('menu').style.display='flex'};$('q').onclic