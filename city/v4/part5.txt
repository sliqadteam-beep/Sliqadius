k=()=>{$('menu').style.display='none';$('quit').style.display='flex'};$('menub').onclick=()=>{st.paused=true;$('menu').style.display='flex'};$('save').onclick=()=>{saveGame();toast('Saved')};$('pause').onclick=()=>{st.paused=!st.paused;$('pause').textContent=st.paused?'>':'II'};$('speed').onclick=()=>{st.speed=st.speed===1?2:st.speed===2?4:1;$('speed').textContent=st.speed+'x'};const keys={};addEventListener('keydown',e=>{if(['INPUT','BUTTON'].includes(document.activeElement?.tagName))return;keys[e.key.toLowerCase()]=1;if(e.key==='Escape'){if($('settings').style.display==='flex'){$('settings').style.display='none';$('menu').style.display='flex'}else if($('menu').style.display==='flex'){$('menu').style.display='none';st.paused=false}else{st.paused=true;$('menu').style.display='flex'}}});addEventListener('keyup',e=>keys[e.key.toLowerCase()]=0);let touches=new Map(),touchBase=null,touchTimer=null,touchStart=null;R.domElement.addEventListener('pointerdown',e=>{if(e.pointerType!=='touch')return;touches.set(e.pointerId,{x:e.clientX,y:e.clientY});if(touches.size===1){touchStart={id:e.pointerId,x:e.clientX,y:e.clientY,moved:false};let ev={clientX:e.clientX,clientY:e.clientY,button:0};touchTimer=setTimeout(()=>{if(touches.size===1&&touchStart&&!touchStart.moved){beginInput(ev,pick(ev));touchTimer=null}},140)}else{if(touchTimer){clearTimeout(touchTimer);touchTimer=null}drag=null;pg.clear();let a=[...touches.values()].slice(0,2),dx=a[1].x-a[0].x,dy=a[1].y-a[0].y;touchBase={mx:(a[0].x+a[1].x)/2,my:(a[0].y+a[1].y)/2,d:Math.hypot(dx,dy),dist,az,target:target.clone()};e.preventDefault();e.stopImmediatePropagation()}},{capture:true});R.domElement.addEventListener('pointermove',e=>{if(e.pointerType!=='touch'||!touches.has(e.pointerId))return;let old=touches.get(e.pointerId);touches.set(e.pointerId,{x:e.clientX,y:e.clientY});if(touchStart&&touchStart.id===e.pointerId&&Math.hypot(e.clientX-touchStart.x,e.clientY-touchStart.y)>8){touchStart.moved=true;if(touchTimer){clearTimeout(touchTimer);touchTimer=null}}if(touches.size>=2&&touchBase){let a=[...touches.values()].slice(0,2),mx=(a[0].x+a[1].x)/2,my=(a[0].y+a[1].y)/2,d=Math.hypot(a[1].x-a[0].x,a[1].y-a[0].y);dist=C(touchBase.dist*(touchBase.d/Math.max(20,d)),24,145);let dx=mx-touchBase.mx,dy=my-touchBase.my;az=touchBase.az-dx*.004;let sc=touchBase.dist*.002;target.x=touchBase.target.x+(-Math.sin(az)*dy-Math.cos(az)*dx)*sc;target.z=touchBase.target.z+(Math.cos(az)*dy-Math.sin(az)*dx)*sc;e.preventDefault();e.stopImmediatePropagation()}},{capture:true});R.domElement.addEventListener('pointerup',e=>{if(e.pointerType!=='touch')return;let was=touches.size,moved=touchStart&&touchStart.id===e.pointerId?touchStart.moved:false;if(was===1&&touchTimer){clearTimeout(touchTimer);touchTimer=null;if(!moved){let ev={clientX:e.clientX,clientY:e.clientY,button:0};beginInput(ev,pick(ev))}}touches.delete(e.pointerId);if(touches.size<2)touchBase=null;if(touchStart&&touchStart.id===e.pointerId)touchStart=null;if(was>=2){e.preventDefault();e.stopImmediatePropagation()}},{capture:true});R.domElement.addEventListener('pointercancel',e=>{if(e.pointerType==='touch'){if(touchTimer){clearTimeout(touchTimer);touchTimer=null}touches.delete(e.pointerId);if(touches.size<2)touchBase=null;touchStart=null;drag=null;pg.clear()}},{capture:true});let pt=performance.now();function loop(t){requestAnimationFrame(loop);let dt=Math.min(.05,(t-pt)/1000);pt=t;let mv=(keys.shift?28:14)*dt,fx=-Math.cos(az),fz=-Math.sin(az),rx=-fz,rz=fx;if(keys.w||keys.arrowup){target.x+=fx*mv;target.z+=fz*mv}if(keys.s||keys.arrowdown){target.x-=fx*mv;target.z-=fz*mv}if(keys.a||keys.arrowleft){target.x-=rx*mv;target.z-=rz*mv}if(keys.d||keys.arrowright){target.x+=rx*mv;target.z+=rz*mv}if(keys.q)az-=dt*1.2;if(keys.e)az+=dt*1.2;if(!st.paused){carsUp(dt);let r=buildings.filter(b=>b.k==='res').length,c=buildings.filter(b=>b.k==='com').length,i=buildings.filter(b=>b.k==='ind').length;st.pop+=(Math.min(r*28,(c*18+i*24)*1.7+30)-st.pop)*dt*.18;st.money+=(st.pop*.05+c*2+i*2)*dt*st.speed;st.happy=C(72-cars.length*.08,35,96)}cam.position.set(target.x+Math.cos(az)*Math.cos(el)*dist,Math.sin(el)*dist,target.z+Math.sin(az)*Math.cos(el)*dist);cam.lookAt(target);R.render(S,cam);$('money').textContent=Math.round(st.money).toLocaleString();$('pop').textContent=Math.round(st.pop).toLocaleString();$('happy').textContent=Math.round(st.happy)+'%';$('traffic').textContent=cars.length>30?'Heavy':cars.length>14?'Moderate':'Light'}loop(pt);addEventListener('resize',()=>{cam.aspect=innerWidth/innerHeight;cam.updateProjectionMatrix();R.setSize(innerWidth,innerHeight)});setTimeout(()=>{if($('load')){$('load').style.display='none';$('menu').style.display='flex'}},650);setTimeout(()=>{let e=$('loaderr');if(e&&$('load').style.display!=='none')e.textContent='Still loading. Check your internet connection and reload the page.'},8000);import('./build7-external-connections.js');
</script></body></html>
