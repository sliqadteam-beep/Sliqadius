name: Deploy SliqServer Website

on:
  push:
    branches: [main]
    paths:
      - 'site/**'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify SliqServer site
        run: |
          set -euo pipefail
          test -f site/index.html
          test -f site/styles.css
          test -f site/404.html
          test -f site/CNAME
          grep -q "SliqServer" site/index.html
          grep -q "SliqServer-Windows.exe" site/index.html
          grep -q "SliqServer-Android.apk" site/index.html
          ! grep -qi "SliqGames" site/index.html
          ! grep -qi "Sliqadius" site/index.html
          ! grep -qi "Sliqado City" site/index.html

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload SliqServer website only
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
